# 2026-07-08 工作记录

## 静态新闻热点聚合站 — 已开发完成(初版)

### 已交付文件(均在项目根 /Users/Zhuanz/WorkBuddy/2026-07-07-23-10-53)
- `index.html` + `assets/css/style.css` + `assets/js/app.js` — 前端(固定框架, 浅色响应式, 16 领域标签 + 历史日期选择器 + 搜索)
- `scripts/fetch.js` — Node 抓取脚本: 调 DailyHotApi, 16 领域归一化(专属平台直接映射 + 综合平台关键词过滤), 写 data/snapshots/<时间戳>.json + 更新 data/manifest.json + 30 天清理; 每源独立 try/catch; 零依赖(内置 fetch)
- `.github/workflows/fetch.yml` — **每两天** UTC 01:00(北京 09:00) + 手动触发, 提交 data/(cron 已改 `"0 1 */2 * *"`)
- `package.json` / `.gitignore` / `README.md` / `PLAN.md`

### 关键技术决策(实施期确认)
- DailyHotApi 平台清单已核实(微博/知乎/百度/抖音/B站/36氪/少数派/IT之家/掘金/酷安/爱范儿/V2EX/果壳/微信读书/简书/豆瓣/澎湃/头条/腾讯/新浪/网易/虎嗅/虎扑/NGA/游戏板等)。注意:该列表**无 github/hackernews 路由**, 故"人工智能"用科技类平台+关键词过滤。
- 默认实例 `https://api-hot.imsyy.top`(维护者公开实例), 经 DAILYHOT_API_BASE Secret 注入; 可自托管 Cloudflare Workers 兜底。
- 快照文件名带 `.json` 后缀(修正过: 原先无后缀)。

### 验证情况
- mock 模式 dry-run 通过: 16 领域每领域 6–13 条, 字段正确, manifest 正确, 补齐逻辑生效(B 类自动补到 6)。
- `node --check assets/js/app.js` 通过; 静态服务器下 index.html/css/js/manifest/snapshot 均 200。
- ⚠️ 沙箱网络被拦截, 真实抓取 total=0, 无法在此播种真实数据。仓库内 data/ 目前是 **mock 占位数据**, 用于本地预览。真实数据由 GitHub Actions(有网络)自动抓取覆盖。

### 给用户的交付要点
- 部署: 建公开仓 → push → Settings/Pages 选 main/(root) → 加 Secret DAILYHOT_API_BASE → Actions 手动触发首次抓取。
- 本地预览: `DAILYHOT_API_BASE=mock node scripts/fetch.js` 然后 `python3 -m http.server`。
- 生产前用真实数据覆盖: 本地联网跑 `node scripts/fetch.js`(读 DAILYHOT_API_BASE) 或靠 Actions。
- 当前仓库 data/ 为占位 mock, 部署前务必替换为真实数据(否则 Pages 先显示占位新闻直到首次 Action 覆盖)。

## 部署进展 2026-07-08 (晚) — 卡在"集成只读"

用户最新指令: "先推送代码, 再进行 action 创建"。已按此理解拆分步骤(先推站点代码不含 workflow, 再推 workflow 作为"创建 action")。

- 用户已手动在 GitHub 创建公开仓库 `liumingmusic/news-hot-site`。
- 再次用 GitHub MCP `push_files` 推送(8 文件, 不含 workflow), 返回 **403 Resource not accessible by integration**: 仓库存在, 但**当前连接的集成只有读权限, 无法创建/提交文件**(与早前建仓 403 同根因)。
- **结论**: 集成无法写入该仓库。沙箱可直接连 GitHub API(早前测过 api.github.com 可达, 但 DailyHotApi 实例被沙箱网络拦截)。
- **给用户的两条路**:
  - A(推荐, 全自动): 用户给 GitHub classic PAT(repo + workflow 权限), 我直接用沙箱 git + 该 token 按"先代码→后 Action"两步推送, 并可一并开 Pages/加 Secret/触发首次抓取。token 仅本会话内用, 不落盘。
  - B(用户本机跑): 已写好 `deploy.sh`(支持 `code`/`action`/`all` 三模式 + PAT 或 gh 两种凭证), 用户本机一条命令推送; 但开 Pages/加 Secret/触发首次抓取仍需用户在 GitHub 点几下。
- 已在工作区 `git init` 并提交就绪状态(剔除 mock data, 9 个文件), 并生成 `deploy.sh`。
- 站点的 16 领域映射、前端、抓取脚本、workflow 均已就绪, 仅差"推到 GitHub"这一步(受集成只读限制)。
